---
# cicd/ansible/inventory/hosts.yml
# Ansible CN 자체에서 실행 (localhost 연결)
# SSM Send-Command 로 CN 에 직접 접속 → localhost 플레이북 실행

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3
